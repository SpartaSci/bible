## Exercise 1
Provide an equivalent definition of the **Decisional Diffie–Hellman Assumption** by using an experiment that outputs 0 or 1.


$G = <g>$ of order $m$ prime
given $g, g^x, g^y, g^z$
$z=xy$
DDH a, gencyclGroup(1^\lmabda)

1. (G, g, m) <- GenCyclGroup(1^\lambda)
2. b \$<- {0,1}
3. x,y,z <- Z_m ()
4. Compute g^x, g^y, g^(b * xy + (1 - b)z
5. give a,b,c to A
6. A output b \in {0,1}
7. return 1 if b=0, 0 otherwise



the probabilit that the adversary win must me less thant 1/2 plus the negl(\lambda)



## Exercise 2
Prove that:
1. Perfect indistinguishability implies statistical indistinguishability.
2. Statistical indistinguishability implies computational indistinguishability.


Perfect --> statistical --> computational

Def
1. Let X,Y be two discrete random variables
Then X,Y are perf. ind. if $Pr[X=x]=Pr[Y=y]$ for some x

2. X,Y discrete random variables. Lambda
Then X,Y are stat.ind. if Exist negl(\lambda) 

$$
\sum_x |P r[X = x] − P r[Y = y]| ≤ negl(λ)
$$

3. Let X and Y be discrete random variables depending on the security parameter λ.
Then X and Y are computationally indistinguishable if for every PPT algorithm A there exists a negligible function negl such that
$$|P r[A(X) = 1] − P r[A(Y ) = 1]| ≤ negl(λ)$$


### sol
1. starting from the first we can have X,Y st 
$$
\sum_x |P r[X = x] − P r[Y = y]| = 0
$$


0 is less that negl(\lambda) so it is o course also stat. indis



2. ppt 

# Exercise 3
**A Group for which Computing the Discrete Logarithm is Easy**

Prove that for the cyclic group (Zp , +) (p prime) with generator g (so g is an integer not divisible by p) the Discrete Logarithm Assumption is false.


> The discrete logarithmic assumption (relative to genCyclgroup) says that for every PPT algorithm A there exists a negligible function negl such that $Pr[DLogA,genCyclGroup (1^\lambda ) = 1] ≤ negl(\lambda)$


(Zp, +) 


# Exercise 4

Can the following problem be solved in polynomial time? 
Given a prime p, a value $x \in Z^∗_{p−1}$ and $y = g^x \mod p$, where $g ← Z^∗_p$ , find g.

$x^{-1} \mod (p-1)$
$y^{x^{-1}} \mod p = (g^x)^{x^{-1}} \mod p = g^1 = g$ 
