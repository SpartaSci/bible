---
aliases:
  - allocation unit
---

> **Data clusters** refer to the basic units of data storage on a filesystem where files are stored.  
 
> A **cluster** (also called an allocation unit) is a group of contiguous disk sectors that the filesystem allocates together as a single unit to store file data.