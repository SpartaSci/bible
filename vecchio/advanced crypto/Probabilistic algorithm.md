>Def. (probabilistic algorithm) (informal)
A probabilistic algorithm is "an algorithm" but with the access to some source of random bits. Also a prob.algo. is allowed to fail and return a failure symbol (T rovesciata)

