
Padding Oracle On Downgraded Legacy Encryption is a MITM attack that exploits SSL-3 fallback to decrypt data

**POODLE variant**, dec-2014, exploits CBC errors in TLS-1.0-1.2 (so it works even if SSL-3 is
disabled);